public class CarSelect {
	/*
	 * The constructor adds Vehicle objects to the carsList[] array.  
	 * rightArrow() increments through the carsList array, leftArrow() 
	 * decrements through the carsList array returning the current car to display.  
	 * start sets the carChosen which should be retrieved from the getter method in 
	 * the MainGame class.
	 */
}
